<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.state.dao.org.OrgDepartUserDaoI">

	<insert id="insert" parameterType="OrgDepartUserPo">
		INSERT INTO <include refid='public.model' /> ORG_DEPART_USER(
		DEPART_UID_,
		USER_UID_,
		DEPART_ID_,
		USER_ID_,
		ADD_NAME_,
		ADD_NAME_CN_,
		DOC_CREATED_
		) VALUES(
		#{departUid ,jdbcType=VARCHAR},
		#{userUid,jdbcType=VARCHAR},
		#{departId ,jdbcType=VARCHAR},
		#{userId,jdbcType=VARCHAR},
		#{addName, jdbcType=VARCHAR},
		#{addNameCn, jdbcType=VARCHAR},
		#{docCreated, jdbcType=TIMESTAMP}
		)
	</insert>
	
<delete id="deleteByDepart" parameterType="string">
    delete from <include refid='public.model' /> ORG_DEPART_USER where DEPART_ID_ in (#{departUids, jdbcType=VARCHAR})
  </delete>
  <delete id="deleteByUser" parameterType="string">
    delete from <include refid='public.model' /> ORG_DEPART_USER where USER_UID_ in (#{userUids, jdbcType=VARCHAR})
  </delete>
</mapper>